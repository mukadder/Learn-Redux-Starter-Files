const min = process.env.MIN_EXT || '';
const DarkTheme = require(`../../dist/DarkTheme${min}`).default;
const LightTheme = require(`../../dist/LightTheme${min}`).default;

export {
  DarkTheme,
  LightTheme
};
